# Core ML and Torch
torch==2.2.1
torchvision==0.17.1

# Gradio for UI
gradio==5.23.3

# Utility
numpy
Pillow

# For tqdm progress bars (used in training notebook)
tqdm

# If needed for Jupyter
ipykernel

# For CUDA support (optional but recommended)
torchsummary

# NOTE: Make sure to install torch with the appropriate CUDA version for your setup.
# Visit: https://pytorch.org/get-started/locally/
